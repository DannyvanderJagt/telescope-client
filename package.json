{
  "name": "thesuitcase-telescope-client",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src --out-dir dist -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "rethinkdb-websocket-client": "^0.5.0"
  }
}
